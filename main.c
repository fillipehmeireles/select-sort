#include<stdio.h>

int main() {
	int arr[] = {5,1,3,6,4,7,2};
	for(int i = 0; i < sizeof(arr)/sizeof(arr[0]); i++) {
		for(int j = i; j < sizeof(arr)/sizeof(arr[0]); j++)
		{
			int smaller_n_index;
			int smaller;
			if(arr[i] > arr[j])

		}
		printf("\n");
	}
}
